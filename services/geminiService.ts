// この機能は無効化されました。
// AI分析機能が必要な場合は、以前のバージョンを参照してください。

import { ServerNode } from "../types";

export const analyzeClusterStatus = async (_server: ServerNode): Promise<string> => {
  return "AI分析機能は現在無効化されています。";
};